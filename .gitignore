node_modules/
.worktrees/
venv/
.venv/

# Audio files (large, except benchmark test corpora which must be tracked)
*.m4a
*.wav
*.mp3
!benchmark/test_corpus/*.m4a
!benchmark/test_corpus/*.wav

# ONNX models
*.onnx

# ONNX Runtime WASM files (copied to public/ via postinstall)
web/frontend/public/*.wasm
web/frontend/public/*.mjs

# Data artifacts
data/*.log
