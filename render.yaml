services:
  - type: web
    name: tarteel
    runtime: node
    buildCommand: cd web/frontend && npm ci && npm run build && npm run build:server
    startCommand: cd web/frontend && npm start
    envVars:
      - key: PORT
        value: "5000"
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/health
