[project]
name = "offline-tarteel"
version = "0.1.0"
description = "Offline Quran verse recognition"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "python-Levenshtein",
    "soundfile",
    "librosa",
]

[project.optional-dependencies]
dev = ["pytest"]
